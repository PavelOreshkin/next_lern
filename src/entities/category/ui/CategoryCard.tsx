import React, { memo } from "react";

type CategoryCardProps = {
  icon: any;
  link: any;
  name: any;
};

const CategoryCard = ({ icon, link, name }: CategoryCardProps) => {
  return <>component</>;
};

export default memo(CategoryCard);
